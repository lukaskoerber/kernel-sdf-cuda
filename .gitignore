build/
*.o
*.obj
*.so
*.a
*.dylib
*.exe

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.ipynb_checkpoints/

# Data: only data/test/ is committed (small reference omegas). Everything else
# (raw inputs, prep binaries, computed Omegas) is too large to track.
data/*
!data/test/

# Local Claude Code settings.
.claude/

# Reference: vendored copy of upstream ckpca, but never its embedded .git.
reference/**/.git
reference/**/.git/**
reference/**/__pycache__/
reference/**/.DS_Store
reference/**/*.egg-info/

# Editor / OS
.vscode/
.idea/
.DS_Store

# CUDA / compile_commands
compile_commands.json
.cache/
